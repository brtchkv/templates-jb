export enum NonAuthRoutes {
    login = "/login",
    register = "/register",
    landing = "/",
    unauthorized = "/error",
};

export enum AuthRoutes {
    profile = "/profile",
    myCourses = "/my",
    logout = "/logout",
};
